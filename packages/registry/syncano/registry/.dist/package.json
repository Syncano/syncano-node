{
  "name": "@syncano/registry-registry-socket",
  "license": "MIT",
  "scripts": {
    "build": "npm install && npm run build:src && npm run build:env",
    "build:src": "sh ./bin/compile",
    "build:env": "sh ./bin/compile-env"
  },
  "dependencies": {
    "@syncano/core": "0.11.0",
    "debug": "^3.1.0",
    "fs-extra": "^5.0.0",
    "semver": "^5.4.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },
  "babel": {
    "presets": [
      [
        "env",
        {
          "targets": {
            "node": "8"
          }
        }
      ]
    ]
  }
}
