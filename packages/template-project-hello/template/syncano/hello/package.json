{
  "license": "MIT",
  "dependencies": {
    "@syncano/core": "latest"
  },
  "scripts": {
    "build": "yarn && ./bin/compile && ./bin/compile-env",
    "build:src": "./bin/compile",
    "build:env": "./bin/compile-env"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "@syncano/build-es6": "0.4.1"
  },
  "babel": {
    "presets": [
      [
        "env",
        {
          "targets": {
            "node": "8"
          }
        }
      ]
    ]
  }
}
